# Data Directory

Place input CSV recordings here (e.g., crane IMU data).

Expected files:
- `crane_imu_obs_debug.csv` — Default input for `run_csv_replay`
- `crane_imu_obs.csv` — Alternative crane recording
- `crane_imu_obs_debug_1.csv` — Extended debug recording

These files are gitignored due to large size. Copy them manually or generate via data collection.
